# Database path (SQLite by default, set to postgresql://... for PostgreSQL)
DB_PATH=officeclauset.db

# JWT secret key (change in production!)
JWT_SECRET=change-me-to-a-random-secret

# JWT expiry in hours
JWT_EXPIRY=24

# Upload directory for clothing item images
UPLOAD_DIR=uploads
